[development]

// Configuración de la conexión a base de datos

db.hostname = "localhost"
db.username = "root"
db.password = ""
db.database = "autoload"

mail.logMailContent = "1"

[testing]

// Configuración de la conexión a base de datos

db.hostname = "localhost"
db.username = "root"
db.password = ""
db.database = "autoload_test"

mail.logMailContent = "1"

[production]

// Configuración de la conexión a base de datos

db.hostname = "localhost"
db.username = "root"
db.password = ""
db.database = "autoload"

mail.logMailContent = "0"

[development : testing : production]

app.name = "autoload"

users.paginator.itemsPerPage = "2"

i18n.path = {PROJECT_PATH} + "/application/configs/locale/"
i18n.defaultLang = "es"

mail.templatesPath = {PROJECT_PATH} + "/templates/mail/"

html.templatesPath = {PROJECT_PATH} + "/templates/html/"

resources.path = {PROJECT_PATH} + "/resources/"